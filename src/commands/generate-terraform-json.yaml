# src/commands/generate-terraform-json.yaml
---

description: |
  convert binary terraform plan into json for analysis by security tools

parameters:

  working-directory:
    description: specific folder in which to run the commands
    type: string
    default: '.'

steps:
  - run:
      name: Generate json formatted version of plan output file
      working_directory: << parameters.working-directory >>
      command: terraform show -json tfplan.binary | jq '.' > tfplan.json
